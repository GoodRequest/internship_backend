export enum Gender {
    MALE = 'male',
    FEMALE = 'female'
}

export enum Order {
    ASC = 'asc',
    DESC = 'desc'
}

export enum TimeUnit {
    SECOND = 'second',
    MINUTE = 'minute',
    HOUR = 'hour',
    DAY = 'day'
}